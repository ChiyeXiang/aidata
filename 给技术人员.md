# AxisX / 子午象限官网 V10 — 技术交付说明

交付日期：2026-09-01  
生产地址：`https://www.ziwuxx.com/ai/data/`  
技术形态：Vite + React 纯前端 SPA；生产环境不运行 Node，不需要数据库。

## 1. 交付内容

| 路径 | 用途 |
|---|---|
| `dist/` | 已构建静态网站，可直接上传到 Nginx |
| `src/` | React 页面、路由、中英文内容与视觉系统 |
| `public/` | 品牌图片、V10 海报、模块视频与页面背景视频 |
| `scripts/render-meridian.mjs` | 全部中英文视频和海报生成脚本 |
| `scripts/assets/` | 视频渲染所需字体 |
| `vite.config.ts` | `dist` 输出和 `/ai/data/` 部署子路径 |
| `nginx.example.conf` | SPA fallback 与缓存配置 |

`node_modules/` 不进入交付包，执行 `npm ci` 即可恢复。

## 2. V10 业务体系

- STEM 原创 Challenge Set：数学、物理、化学、生物、计算机科学专家原创出题。
- 每道题配套参考推导、评分量规、答案唯一性检查和符号、数值或程序化验证器。
- 通过独立专家复算、模型试跑、难度分层、可检索性与污染风险筛查验证任务有效性。
- 金融、法律、医学任务保留来源证据、专业边界、双评与领域裁决。
- Agent 任务记录计划、工具调用、观察、回退、修正、停止条件与失败归因。
- MLE / MLS 评测覆盖环境、实验、产物、指标、消融、泛化证据与独立复跑。
- ProgramBench、DeepSWE、FrontierSWE 型任务覆盖程序行为重建、真实仓库长程工程、隐藏测试和程序化验证。
- 首页、能力、交付、案例、Meridian、质量、Roadmap、公司和联系页均有中英文完整叙事。

公开 Benchmark 名称只描述可对齐的任务方法；除非另有明确说明，不宣称官方合作、授权或所有权。医学内容不提供临床诊断，法律内容不替代法律意见。

## 3. 视频系统

```bash
npm run render:videos
```

生成内容：

- 12 个中英文模块视频：领域专家、STEM、Agent 轨迹、MLE / MLS、ProgramBench、DeepSWE / FrontierSWE
- 2 个中英文工作流总览视频
- 10 个中英文页面背景视频：首页、能力、案例、Meridian、未来
- 18 张中英文模块与领域海报

目录：

```text
public/meridian/clips/
public/meridian/backgrounds/
public/meridian/v10/
```

网页中的 `<video>` 均使用 `autoplay muted loop playsinline`，没有 `controls`、播放按钮或播放器边框。模块视频 12 秒以上，背景视频 11 秒，总览视频 26 秒以上。

视频脚本使用连续帧序列生成产品操作画面，并通过项目本地 `sharp` 与 `@ffmpeg-installer/ffmpeg` 完成帧渲染和 MP4 编码；如生产机器已有可用 `ffmpeg`，也可通过 `FFMPEG=/path/to/ffmpeg npm run render:videos` 指定。

## 3.1 全站环境背景动效

- 首页、能力、交付、案例、案例详情、Meridian、质量、未来、公司、联系及 404 均通过 `PageBackdrop` 配置独立背景图。
- 视觉由业务海报或真实场景照片、边缘蒙版、低透明度网格和轨道光点组合，不影响正文交互。
- 桌面端包含缓慢景深漂移与滚动视差；移动端自动降低透明度与运动范围。
- `prefers-reduced-motion: reduce` 下全部背景动画停止，保留静态视觉层。
- 路由与图片映射位于 `src/main.jsx` 的 `PageBackdrop`；动效位于 `src/styles.css` 的 `V10.1` 区域。

## 4. 构建与预览

建议 Node.js 20 或 22、npm 10+。

```bash
npm ci
npm run dev
npm run build
npm run preview
```

构建后必须存在：

```text
dist/index.html
dist/assets/index-*.js
dist/assets/index-*.css
dist/meridian/clips/
dist/meridian/backgrounds/
dist/meridian/v10/
```

## 5. Nginx 上线

把 `dist/` 内全部内容上传到：

```text
/var/www/ziwuxx.com/ai/data/
```

关键 fallback：

```nginx
location /ai/data/ {
    try_files $uri $uri/ /ai/data/index.html;
}
```

这条规则不能省略，否则刷新案例详情或双语深层路由会 404。完整配置见 `nginx.example.conf`。

## 6. 主要路由

| 英文（默认） | 中文 |
|---|---|
| `/ai/data/` | `/ai/data/zh` |
| `/ai/data/services` | `/ai/data/zh/services` |
| `/ai/data/delivery` | `/ai/data/zh/delivery` |
| `/ai/data/work` | `/ai/data/zh/work` |
| `/ai/data/meridian` | `/ai/data/zh/meridian` |
| `/ai/data/quality` | `/ai/data/zh/quality` |
| `/ai/data/future` | `/ai/data/zh/future` |
| `/ai/data/about` | `/ai/data/zh/about` |
| `/ai/data/contact` | `/ai/data/zh/contact` |

案例详情使用 `/work/<slug>`，中文增加 `/zh` 前缀。

## 7. 修改位置

| 修改内容 | 文件 |
|---|---|
| 能力、案例、流程、质量、Roadmap、领域 | `src/content.js` |
| 页面结构、路由、邮件入口与视频调用 | `src/main.jsx` |
| 排版、响应式、背景视频与动效 | `src/styles.css` |
| Meridian 视频画面和文案 | `scripts/render-meridian.mjs` |
| 部署子路径 | `vite.config.ts` |

不要直接编辑 `dist/assets/` 的 hash 文件；修改源码后重新运行 `npm run build`。

## 8. 上线验收

- [ ] 默认英文 `/ai/data/` 与中文 `/ai/data/zh` 正常。
- [ ] 英文页面除“中文”语言按钮外没有中文残留。
- [ ] 七个双语案例详情可进入并可直接刷新。
- [ ] STEM Challenge Set 案例、能力、质量、Meridian 和 Roadmap 叙事一致。
- [ ] 12 个模块视频、2 个总览视频和 10 个背景视频均静音自动循环。
- [ ] 页面 DOM 中没有视频 `controls`、播放按钮或播放框。
- [ ] 每条路由均存在一个 `data-backdrop` 环境背景层，且图片资源返回 200。
- [ ] 移动端背景不遮挡文字，减少动态效果模式下动画停止。
- [ ] 所有模块与背景视频均超过 10 秒。
- [ ] 页面不出现 2D、3D 点云等旧业务入口。
- [ ] CSS、JS、海报和视频在 `/ai/data/` 子路径下无 404。
- [ ] 联系按钮打开邮件客户端，收件人为 `kevin@ziwuxx.com`。
