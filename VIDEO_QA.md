# V10 视频专业性验收记录

验收日期：2026-09-02

## 资产范围

| 类型 | 数量 | 单条时长 | 语言 |
|---|---:|---:|---|
| Meridian 模块视频 | 12 | 12.00 秒 | 英文 6 / 中文 6 |
| 页面背景视频 | 10 | 11.00 秒 | 英文 5 / 中文 5 |
| 工作流总览视频 | 2 | 26.50 秒 | 英文 1 / 中文 1 |

全部 24 个 MP4 已通过 `ffprobe` 完整性与时长检查，均超过 10 秒。

## 专业性检查

- 英文和中文视频分别生成，不使用中文画面复用到英文站。
- STEM 模块覆盖原创题面、参考推导、模型通过率、专家复算、答案唯一性、程序化验证器与可检索性筛查。
- 专家、Agent、MLE / MLS、ProgramBench 与 SWE 模块均补充脱敏演示对象、规模、结果和实时操作记录。
- 模块视频由连续帧序列生成，包含移动指针、扫描线、进度变化和状态更新，减少静态幻灯片感。
- 页面只渲染视频画面，不显示控制条、播放按钮、播放框或浏览器原生 controls。
- 所有视频使用 `autoplay muted loop playsinline`，适合桌面和移动端静音自动循环。
- 背景视频经过降饱和、对比度和裁切处理，保证正文对比度，不与标题争夺注意力。
- 画面标注为产品开发预览 / 内部试运行，避免把建设中能力表述为全部正式上线。

## 重新生成

```bash
npm run render:videos
npm run build
```

当前脚本会优先使用项目本地的 `sharp` 与 `@ffmpeg-installer/ffmpeg`；如果系统已有可用 `ffmpeg`，也可以通过 `FFMPEG=/path/to/ffmpeg` 指定。
